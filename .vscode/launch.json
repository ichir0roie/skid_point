{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Chrome",
            "request": "launch",
            "type": "chrome",
            "webRoot": "${workspaceFolder}",
            "url": "http://localhost:3000",
            "userDataDir": "${workspaceFolder}/test/chromeUserDataDir"
        },
    ]
}